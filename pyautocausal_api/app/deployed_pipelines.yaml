- example_graph:
    description: "Example graph to demonstrate the API"
    graph_uri: s3://pyautocausal-pipelines/example_graph.pkl
    required_columns: 
      - id_unit
      - t 
      - treat
      - y
      - post
    optional_columns: []